# Set is the collection of the unordered items.
# Each element in the set is must be unique and immutable.
a={1,2,2,3,4,"hello","world","hello"} # Repeated elements in the set is stored only once.
print(a)
print(type(a))
print(len(a))
b=set() #Empty set syntax
print(b)